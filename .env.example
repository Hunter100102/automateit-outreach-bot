# === REQUIRED ===
# Azure Bing Web Search API key (create a resource in Azure Cognitive Services)
BING_SEARCH_API_KEY=your_bing_api_key_here

# SendGrid API key (e.g., from your SendGrid account)
SENDGRID_API_KEY=your_sendgrid_key_here

# The "from" details for your emails (must be a verified sender in SendGrid)
FROM_EMAIL=william@automatingsolutions.com
FROM_NAME=William Cody Hunter

# Public base URL where this app is hosted (Render/Heroku/Vercel). Used for unsubscribe links.
PUBLIC_BASE_URL=https://your-domain-or-render-app.onrender.com

# Your physical postal address for CAN-SPAM footer
COMPANY_ADDRESS_1=
COMPANY_ADDRESS_2=
COMPANY_CITY=Stone Mountain
COMPANY_STATE=GA
COMPANY_ZIP=30083
COMPANY_COUNTRY=USA

# Link for booking (Calendly)
CALENDLY_URL=https://calendly.com/william-automatingsolutions/30min

# === OPTIONAL ===
# NeverBounce API key if you want real-time verification. If omitted, regex validation is used.
NEVERBOUNCE_API_KEY=

# Rate limits
REQUESTS_PER_MINUTE=30

# === SEARCH CONFIG ===
# Semicolon-separated list of US cities or states to target (used to build search queries)
TARGET_LOCATIONS="Atlanta, GA; Sandy Springs, GA; Tucker, GA; Decatur, GA; Roswell, GA"

# Semicolon-separated industries/keywords to search for
TARGET_INDUSTRIES="moving company; salon; spa; HVAC; auto repair; cafe; contractor"

# Additional keywords appended to searches to find contact pages
SEARCH_KEYWORDS="contact; team; leadership; about; owners; management; email"

# Max number of search results per query (Bing returns paginated results; we use top N)
MAX_RESULTS_PER_QUERY=15

# Max pages to crawl per domain (to avoid heavy scraping)
MAX_PAGES_PER_DOMAIN=3
